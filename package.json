{
  "name": "@phototology/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for the Phototology AI vision API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "init": "bin/init.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests",
    "prepublishOnly": "pnpm build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
